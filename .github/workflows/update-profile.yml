name: Atualizar Perfil

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: true

      - name: Instalar jq
        run: sudo apt-get update && sudo apt-get install -y jq

      # Atualizar Últimos Projetos
      - name: Atualizar Projetos
        run: |
          # Pegar 3 pastas mais recentes
          repos=$(curl -s https://api.github.com/repos/edson-syl/projetos/contents/fullstack \
            | jq -r '
                map(select(.type == "dir" and .name != "README.md"))
                | sort_by(.name)
                | reverse
                | unique_by(.name)
                | .[0:3]
                | .[]
                | "<td align=\"center\"><a href=\"https://github.com/edson-syl/projetos/tree/main/fullstack/" + .name + "\"><img src=\"https://img.shields.io/badge/" + .name + "-blue?style=for-the-badge\" height=\"60\" /></a></td>"
            ')

          # Substituir conteúdo entre as tags, sem repetir
          awk -v r="$repos" '
            /<!--START_SECTION:repos-->/ {print; print "<tr>" r "</tr>"; skip=1; next}
            /<!--END_SECTION:repos-->/ {skip=0; print; next}
            skip != 1 {print}
          ' README.md > README.tmp && mv README.tmp README.md


      # Atualizar Frase do Dia
      - name: Frase Aleatória
        run: |
          frases=("Nunca pare de aprender" "Código limpo é código feliz" "Errar faz parte do deploy" "Pequenos commits, grandes resultados")
          frase=$(printf "%s\n" "${frases[@]}" | shuf -n 1)
          awk -v f="$frase" '
            /<!--START_SECTION:frase-->/ {print; print f; next}
            /<!--END_SECTION:frase-->/ {print; next}
            !/<!--START_SECTION:frase-->/ && !/<!--END_SECTION:frase-->/ {print}
          ' README.md > README.tmp && mv README.tmp README.md
          
      # Commit e Push
      - name: Commit e Push
        run: |
          git config user.name github-actions
          git config user.email actions@github.com
          git add README.md
          git commit -m "🤖 Atualização automática do perfil" || echo "Sem mudanças"
          git push
