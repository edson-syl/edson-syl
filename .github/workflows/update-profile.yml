name: Atualizar Perfil

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Atualizar Projetos
        run: |
          repos=$(curl -s https://api.github.com/users/edson-syl/repos?sort=updated | jq -r '.[0:5][] | "- 🔗 [" + .name + "](" + .html_url + ")"')
          sed -i "/<!--START_SECTION:repos-->/, /<!--END_SECTION:repos-->/c\<!--START_SECTION:repos-->\n$repos\n<!--END_SECTION:repos-->" README.md

      - name: Frase Aleatória
        run: |
          frases=("Nunca pare de aprender" "Código limpo é código feliz" "Errar faz parte do deploy" "Pequenos commits, grandes resultados")
          frase=${frases[$RANDOM % ${#frases[@]}]}
          sed -i "/<!--START_SECTION:frase-->/, /<!--END_SECTION:frase-->/c\<!--START_SECTION:frase-->\n$frase\n<!--END_SECTION:frase-->" README.md

      - name: Atualizar Data
        run: |
          data=$(date)
          sed -i "/<!--START_SECTION:data-->/, /<!--END_SECTION:data-->/c\<!--START_SECTION:data-->\n$data\n<!--END_SECTION:data-->" README.md

      - name: Commit e Push
        run: |
          git config user.name github-actions
          git config user.email actions@github.com
          git add .
          git commit -m "🤖 Atualização automática do perfil" || echo "Sem mudanças"
          git push
